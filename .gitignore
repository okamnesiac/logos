# The spec repo only tracks the workspace shell + spec/.
# agent/, config/, memory/, runtime/ are sibling domains owned elsewhere
# (own repos, or unmanaged folders). Whitelist what we track.

/*
!/.gitignore
!/.gitattributes
!/README.md
!/CLAUDE.md
!/AGENTS.md
!/spec/
